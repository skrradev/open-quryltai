---
# Single-server UFW firewall + fail2ban.
# Default deny incoming / allow outgoing; open SSH, HTTP, HTTPS, and the
# Tailscale interface. SSH rule is added BEFORE enabling UFW to avoid lockout.
- name: Install UFW and fail2ban
  apt:
    name:
      - ufw
      - fail2ban
    state: present
    update_cache: yes

- name: Default deny incoming
  community.general.ufw:
    direction: incoming
    policy: deny

- name: Default allow outgoing
  community.general.ufw:
    direction: outgoing
    policy: allow

- name: Allow SSH (port 22)
  community.general.ufw:
    rule: allow
    port: "22"
    proto: tcp

- name: Allow HTTP (port 80)
  community.general.ufw:
    rule: allow
    port: "80"
    proto: tcp

- name: Allow HTTPS (port 443)
  community.general.ufw:
    rule: allow
    port: "443"
    proto: tcp

- name: Allow all traffic on the Tailscale interface
  community.general.ufw:
    rule: allow
    interface: tailscale0
    direction: in

- name: Allow Tailscale WireGuard UDP (direct connections)
  community.general.ufw:
    rule: allow
    port: "41641"
    proto: udp

- name: Enable UFW
  community.general.ufw:
    state: enabled

- name: Ensure fail2ban is running and enabled
  service:
    name: fail2ban
    state: started
    enabled: true
