# Local development server
PORT=8080

# The default example uses the embedded H2 database in PostgreSQL compatibility mode.
# Replace these values with your PostgreSQL connection details when needed.
DATABASE_URL=jdbc:h2:file:./data/quryltai;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
DATABASE_USERNAME=sa
DATABASE_PASSWORD=
