# Used by browser code when making API requests. VITE_ variables are public.
VITE_API_BASE_URL=/api
VITE_GA_MEASUREMENT_ID=

# Used only by the Vite development server to proxy /api requests.
BACKEND_URL=http://localhost:8080
